import { EndOfStringPipe } from './end-of-string.pipe';

describe('EndOfStringPipe', () => {
  it('create an instance', () => {
    const pipe = new EndOfStringPipe();
    expect(pipe).toBeTruthy();
  });
});
